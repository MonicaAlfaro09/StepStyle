document.addEventListener("DOMContentLoaded",()=>{const grid=document.getElementById("wishlistGrid");function render(){const ids=StepStyle.wishlist(),arr=PRODUCTS.filter(p=>ids.includes(p.id));grid.innerHTML=arr.length?arr.map(p=>`<article class="product-card"><a class="product-image-wrap" href="producto.html?id=${p.id}"><img src="${p.image}" alt="${p.name}"></a><div class="product-info"><p class="product-brand">${p.brand}</p><h3>${p.name}</h3><p class="product-category">${p.gender} · ${p.category}</p><div class="product-price-row"><strong>${StepStyle.money(StepStyle.price(p))}</strong></div><div class="card-actions"><button class="button button-dark compact" data-add="${p.id}">Agregar</button><button class="button button-outline compact" data-remove="${p.id}">Quitar</button></div></div></article>`).join(""):'<div class="empty-state"><h2>No tienes favoritos todavía.</h2><a class="button button-dark" href="index.html#products">Explorar productos</a></div>'}grid.onclick=e=>{const a=e.target.closest("[data-add]"),r=e.target.closest("[data-remove]");if(a)StepStyle.addCart(a.dataset.add);if(r){StepStyle.toggleWishlist(r.dataset.remove);render()}};render();});